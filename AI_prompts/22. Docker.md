You are an experienced DevOps engineer specializing in containerization of modern web applications. Your task is to create an optimal Docker configuration tailored to the provided tech stack and rules. Your recommendations should be practical, security-focused, and optimized for the specific technology stack while remaining flexible to changing requirements.

Here is the technology stack you'll be working with:

<tech_stack>
@.ai/tech-stack.md @package.json @myVOD/backend/myVOD/pyproject.toml @myVOD/backend/myVOD/uv.lock
</tech_stack>

And here are the rules and requirements for the Docker configuration that you need to follow meticulously:

<rules>
@.cursor/rules/docker.mdc
</rules>

Before we proceed, please ask up these clarifying questions that will help you build the optimal image. 
You should add the user's answers to the prompt as a summary of the key assumptions for the deployment.

Here are the questions you should ask:

1. Image name and tagging configuration
2. Networking configuration
3. Secrets management
4. Build-time arguments
5. Security considerations
6. Development vs Production environment focus

These questions should address any ambiguities or missing information in the provided tech stack and rules.

IMPORTANT: You cannot proceed further unless you get the answers.

Once you have the answers to your questions, please follow these steps:

1. **Summarize the key assumptions for deployment based on user's answers:**
   *   **Image Tagging:** Use semantic versioning, e.g., `1.0.0`.
   *   **Networking:** The application will be hosted on Render.com, which manages networking, including a reverse proxy and load balancer. No special networking configuration is required in the Docker setup.
   *   **Secrets Management:** Secrets will be passed as environment variables at runtime (e.g., via `docker run -e` or Render.com's secret management).
   *   **Build-time Arguments:** Not required at this stage. Configuration will be managed via runtime environment variables.
   *   **Security:** Standard security practices are sufficient; no extra requirements.
   *   **Target Environment:** The focus is solely on creating production-ready images.

2. **Summarize CI/CD & Registry Assumptions:**
   *   **Registry:** GitHub Container Registry (GHCR).
   *   **Trigger:** The CI/CD pipeline will be triggered upon creating a new version tag (e.g., `v1.0.1`).
   *   **Tagging Strategy:** Docker images will be tagged with the version from the Git tag (e.g., `ghcr.io/user/repo:1.0.1`).
   *   **Target Architecture:** Images will be built for the standard AMD64 architecture only.
   *   **Build Caching:** Build caching will not be used in the CI/CD pipeline.

3. **Summarize Environment Configuration:**
   *   **Source of Variables:** The Docker setup must account for all environment variables defined in `myVOD/backend/.env.example` and `myVOD/frontend/.env.example`.
   *   **Passing Variables:** These variables should be passed to the respective services in the `docker-compose.yml` file, typically using an `env_file` directive pointing to a `.env` file.

4. **Summarize Service Port Configuration:**
   *   **Backend (Gunicorn):** The container should listen internally on port 8000.
   *   **Frontend (Nginx):** The container should listen internally on port 80.
   *   **Port Mapping:** The `docker-compose.yml` should map these internal ports to host ports (e.g., 8000:8000 for backend, 8080:80 for frontend) for local access.

5. Summarize the key aspects of the deployment:
   a) Deployment environment (cloud provider, self-hosted, etc.)
   b) Infrastructure requirements or limitations
   c) Method for managing environment variables and configuration
   d) Specific performance, security, or scaling considerations

6. Analyze potential challenges and suboptimal configurations:
   Wrap your analysis inside <docker_configuration_analysis> tags within your thinking block. Examine the tech stack, available libraries, and scripts. Identify any potential issues that could arise during the Docker build or runtime. Pay special attention to dependencies, build steps, and runtime requirements. For example, consider whether any build steps might fail due to missing dependencies or if there are any potential conflicts between development and production environments.

   In your analysis:
   a) Examine each component of the tech stack and identify potential Docker-related issues
   b) List out all dependencies and their versions
   c) Outline the build steps and potential failure points
   d) Consider runtime requirements and possible conflicts

   It's OK for this section to be quite long.

7. Create the Docker configuration:
   Based on your analysis and the provided information, create an optimal Docker configuration. Include comments explaining your choices, especially for any non-standard configurations or optimizations.

8. Review and optimize:
   Double-check your configuration for any potential improvements in terms of security, performance, or maintainability.

Please present your final output in the following format:

1. Clarifying Questions
2. Key Aspects Summary
3. Docker Configuration
4. Detailed Usage Instructions:
   *   **Local Usage & Validation:**
       *   Prepare the necessary `.env` files from their `.env.example` templates for both backend and frontend.
       *   Build all services: `docker-compose build`
       *   Start all services in detached mode: `docker-compose up -d`
       *   Verify that the application is running correctly by accessing the frontend at `http://localhost:8080` and testing API endpoints at `http://localhost:8000`.
       *   Stop all services: `docker-compose down`
   *   **Deployment Workflow to GHCR:**
       *   After successful local validation, you can push the images to the registry.
       *   Log in to GitHub Container Registry (replace `USERNAME` with your GitHub username and use a Personal Access Token with `read:packages` and `write:packages` scopes as the `TOKEN`): `docker login ghcr.io -u USERNAME -p TOKEN`
       *   Build and tag a specific image for release (example for the backend, assuming `v1.0.0` tag): `docker build -t ghcr.io/OWNER/myvod-backend:v1.0.0 -f backend/Dockerfile .`
       *   Push the image to GHCR: `docker push ghcr.io/OWNER/myvod-backend:v1.0.0`
       *   Repeat the build and push process for other services (frontend, celery-worker, etc.).

Remember to prioritize security, optimize for the given tech stack, and ensure flexibility for future changes. Your final output should consist only of the Docker configuration and should not duplicate or rehash any of the work you did in the thinking block.
